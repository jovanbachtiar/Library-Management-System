package library;

public class FictionBook extends Book {
    public FictionBook(String title, String author, String publicationDate) {
        super(title, author, publicationDate);
        // Any additional initialization specific to fiction books
    }
}